import { render, screen } from '@testing-library/react';
import App from '../App';

// TODO: Add meaningful tests as pages are implemented
test('renders without crashing', () => {
  render(<App />);
});
